{% extends 'base_back.html.twig' %}

{% block title %}Forumpost{% endblock %}

{% block body %}
    <h1>Forumpost</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ forumpost.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ forumpost.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ forumpost.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_forumpost_index') }}">back to list</a>

    <a href="{{ path('app_forumpost_edit', {'id': forumpost.id}) }}">edit</a>

    {{ include('forumpost/_delete_form.html.twig') }}
{% endblock %}
